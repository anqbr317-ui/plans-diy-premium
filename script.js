const cart=JSON.parse(localStorage.getItem("bricolageCart")||"[]");const cartPanel=document.querySelector("#cartPanel"),cartOverlay=document.querySelector("#cartOverlay"),cartItems=document.querySelector("#cartItems"),cartCount=document.querySelector("#cartCount"),cartTotal=document.querySelector("#cartTotal"),openCartButton=document.querySelector("#openCart"),closeCartButton=document.querySelector("#closeCart"),clearCartButton=document.querySelector("#clearCart"),checkoutButton=document.querySelector("#checkoutButton"),menuButton=document.querySelector("#menuButton"),navLinks=document.querySelector("#navLinks");function saveCart(){localStorage.setItem("bricolageCart",JSON.stringify(cart))}function formatPrice(v){return new Intl.NumberFormat("fr-FR",{style:"currency",currency:"EUR"}).format(v)}function addToCart(name,price){const e=cart.find(i=>i.name===name);e?e.quantity++:cart.push({name,price,quantity:1});saveCart();renderCart();openCart()}function changeQuantity(index,amount){cart[index].quantity+=amount;if(cart[index].quantity<=0)cart.splice(index,1);saveCart();renderCart()}function removeItem(index){cart.splice(index,1);saveCart();renderCart()}function renderCart(){const count=cart.reduce((s,i)=>s+i.quantity,0),total=cart.reduce((s,i)=>s+i.price*i.quantity,0);cartCount.textContent=count;cartTotal.textContent=formatPrice(total);if(!cart.length){cartItems.innerHTML='<p class="empty-cart">Votre panier est vide.</p>';return}cartItems.innerHTML=cart.map((item,index)=>`<div class="cart-item"><div><h4>${item.name}</h4><span>${formatPrice(item.price)} × ${item.quantity}</span></div><div class="cart-item-actions"><button class="quantity-button" onclick="changeQuantity(${index},-1)">−</button><strong>${item.quantity}</strong><button class="quantity-button" onclick="changeQuantity(${index},1)">+</button><button class="remove-item" onclick="removeItem(${index})">✕</button></div></div>`).join("")}function openCart(){cartPanel.classList.add("open");cartOverlay.classList.add("visible");document.body.classList.add("cart-open");cartPanel.setAttribute("aria-hidden","false")}function closeCart(){cartPanel.classList.remove("open");cartOverlay.classList.remove("visible");document.body.classList.remove("cart-open");cartPanel.setAttribute("aria-hidden","true")}document.querySelectorAll(".add-to-cart").forEach(b=>b.addEventListener("click",()=>addToCart(b.dataset.name,Number(b.dataset.price))));function applyFilter(selected){document.querySelectorAll(".filter").forEach(b=>b.classList.toggle("active",b.dataset.filter===selected));document.querySelectorAll(".product-card").forEach(card=>card.classList.toggle("hidden",selected!=="all"&&card.dataset.category!==selected))}document.querySelectorAll(".filter").forEach(b=>b.addEventListener("click",()=>applyFilter(b.dataset.filter)));document.querySelectorAll("[data-go-filter]").forEach(link=>link.addEventListener("click",()=>setTimeout(()=>applyFilter(link.dataset.goFilter),50)));openCartButton.addEventListener("click",openCart);closeCartButton.addEventListener("click",closeCart);cartOverlay.addEventListener("click",closeCart);clearCartButton.addEventListener("click",()=>{cart.splice(0,cart.length);saveCart();renderCart()});checkoutButton.addEventListener("click",()=>{if(!cart.length){alert("Votre panier est vide.");return}const lines=cart.map(i=>`- ${i.name} × ${i.quantity} : ${formatPrice(i.price*i.quantity)}`),total=cart.reduce((s,i)=>s+i.price*i.quantity,0),subject=encodeURIComponent("Commande Plans Bricolage Premium"),body=encodeURIComponent(`Bonjour,

Je souhaite commander les plans suivants :

${lines.join("\n")}

Total : ${formatPrice(total)}

Mon nom :
Mon adresse e-mail :
Mon message :

Merci.`);window.location.href=`mailto:anqbr317@gmail.com?subject=${subject}&body=${body}`});menuButton.addEventListener("click",()=>navLinks.classList.toggle("open"));document.querySelectorAll(".nav-links a").forEach(l=>l.addEventListener("click",()=>navLinks.classList.remove("open")));renderCart();